language: java
jdk: openjdk13

# install:
#   - mettre une commmande si nécessaire

os:
  - linux
  - osx

env:
    matrix:
      - TARGET="lab1"
      - TARGET="lab2"

script:
  - cd lab1
  - mvn package
