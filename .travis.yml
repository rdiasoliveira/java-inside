language: java
jdk: openjdk13

# install:
#   - mettre une commmande si nécessaire

os:
  - linux
  - osx

env:
    matrix:
      - TARGET="lab1"
      - TARGET="lab2"
      - TARGET="lab3"
      - TARGET="lab4"

script:
  - cd $TARGET
  - mvn package
